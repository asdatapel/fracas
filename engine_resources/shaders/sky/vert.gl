#version 430 core

uniform mat4 model;

layout (location = 0) in vec2 pos;

out vec3 frag_world_pos;

void main()
{
    frag_world_pos = (model * vec4(pos.x, -pos.y, 1, 1)).xyz;
    gl_Position = vec4(pos, 0, 1);
}