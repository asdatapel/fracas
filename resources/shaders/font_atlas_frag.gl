
#version 330 core
out vec4 FragColor;

uniform sampler2D tex;

in vec2 frag_uv;

void main()
{
  float dist = texture(tex, frag_uv).r;
  float alpha = smoothstep(0.1, 0.25, dist);
  FragColor = vec4(1.0, 1.0, 1.0, alpha);
  // FragColor = vec4(frag_uv.x, 0, 0, 1);
}