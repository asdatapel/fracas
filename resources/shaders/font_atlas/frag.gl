
#version 330 core
out vec4 FragColor;

uniform vec4 color;
uniform sampler2D tex;

in vec2 frag_uv;

void main()
{
  float dist = texture(tex, frag_uv).r;
  float alpha = smoothstep(0.1, 0.25, dist);
  FragColor = color * alpha;
}